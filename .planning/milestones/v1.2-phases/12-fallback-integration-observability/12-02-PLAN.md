---
phase: 12-fallback-integration-observability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - klippy/extras/tool_fallback.py
  - tests/test_tool_fallback_extension.py
autonomous: true
requirements: [FALLBACK-04, OBSERVE-01, OBSERVE-02]

must_haves:
  truths:
    - "SHOW_TOOL_FALLBACK_STATE includes a user_defined_backups key in its output snapshot"
    - "The user_defined_backups key shows the same format as the state file: {logical_tool: backup_tool_or_null}"
    - "Undefined tool detection events are visible in Klipper's log system (verified by existing Phase 10 code)"
  artifacts:
    - path: klippy/extras/tool_fallback.py
      provides: "get_status() includes user_defined_backups in status snapshot for SHOW_TOOL_FALLBACK_STATE"
      exports: ["get_status"]
    - path: tests/test_tool_fallback_extension.py
      provides: "Test that SHOW_TOOL_FALLBACK_STATE output includes user_defined_backups key"
  key_links:
    - from: klippy/extras/tool_fallback.py
      to: klippy/extras/tool_fallback_state.py
      via: "get_status() calls state.to_dict() which includes user_defined_backups; get_status() adds it to the top-level snapshot"
      pattern: "user_defined_backups"
    - from: klippy/extras/tool_fallback.py
      to: klippy/extras/tool_fallback.py
      via: "SHOW_TOOL_FALLBACK_STATE calls get_status() which returns the full snapshot"
      pattern: "cmd_SHOW_TOOL_FALLBACK_STATE.*get_status"
---

<objective>
Expose user-defined backup mappings in the SHOW_TOOL_FALLBACK_STATE output and verify that undefined tool detection events are already logged to Klipper's log system.

Purpose: Operators need visibility into user-defined backup mappings through the standard status command, and need confirmation that Phase 10's detection events reach Klipper's log system.

Output: get_status() adds user_defined_backups to the top-level snapshot; verification that Phase 10's respond_info calls reach Klipper logs.
</objective>

<execution_context>
@/home/orion/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/orion/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-fallback-integration-observability/12-CONTEXT.md
@klippy/extras/tool_fallback.py
@klippy/extras/tool_fallback_state.py
@tests/test_tool_fallback_extension.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add user_defined_backups to get_status() snapshot for SHOW_TOOL_FALLBACK_STATE</name>
  <files>klippy/extras/tool_fallback.py</files>
  <action>
    Modify `get_status()` in `klippy/extras/tool_fallback.py` (line ~275) to include `user_defined_backups` in the top-level status snapshot returned by `SHOW_TOOL_FALLBACK_STATE`.

    Current `get_status()` (line 275-288):
    ```python
    def get_status(self, eventtime):
        if self.state is None:
            return {"initialized": False}
        snapshot = self.state.to_dict()
        snapshot["initialized"] = True
        snapshot["configuration"] = asdict(self.config.global_config)
        snapshot["active_logical_tool"] = self._active_logical_tool
        snapshot["selected_physical_tool"] = self._selected_physical_tool
        snapshot["transition_active"] = self._transition_active
        snapshot["sensor_authority"] = self._sensor_status_snapshot()
        snapshot["workflow"] = self._workflow_status_snapshot()
        snapshot["backup_operation_queue"] = self._backup_queue_status_snapshot()
        return snapshot
    ```

    `self.state.to_dict()` already includes `user_defined_backups` at the top level (tool_fallback_state.py line 148-163):
    ```python
    def to_dict(self):
        return {
            "version": self.version,
            "tools": {...},
            "mappings": dict(self.mappings),
            "user_defined_backups": dict(self.user_defined_backups),
        }
    ```

    Per D-08 and D-09: The `user_defined_backups` key is already in `snapshot` from `to_dict()`. What's needed is to ensure it's clearly visible at the top level of the SHOW_TOOL_FALLBACK_STATE output, distinct from the per-tool `user_defined_backup` fields inside `tools`.

    Since `to_dict()` already includes `user_defined_backups` as a top-level key, the output already satisfies FALLBACK-04 and OBSERVE-01. However, per D-13, the output should distinguish between user-defined backups and configured backups. The current `to_dict()` output does this:
    - `user_defined_backups` at top level = user-defined mappings (D-13)
    - `tools[Tn].user_defined_backup` = per-tool user-defined backup (from state schema)
    - `tools[Tn].backups` = configured backup chain

    Verify this is the case by checking the output format. If `user_defined_backups` is already present at the top level from `to_dict()`, no code change is needed — only a test to confirm.

    Action:
    1. Verify that `get_status()` output includes `user_defined_backups` from `to_dict()`
    2. If already present → no code change needed, add test coverage
    3. If missing → add `snapshot["user_defined_backups"] = dict(self.state.user_defined_backups)` to `get_status()`
  </action>
  <verify>
    python3 -c "
from klippy.extras.tool_fallback_state import FallbackState
from types import MappingProxyType
state = FallbackState.from_dict({
    'version': 2,
    'tools': {'T0': {'loaded': True, 'purged': True, 'failed': False, 'backups': ['T1'], 'user_defined_backup': None}, 'T1': {'loaded': True, 'purged': True, 'failed': False, 'backups': [], 'user_defined_backup': None}},
    'mappings': {'T0': 'T0', 'T1': 'T1'},
    'user_defined_backups': {'T0': 'T1'}
})
d = state.to_dict()
assert 'user_defined_backups' in d, 'user_defined_backups missing from to_dict()'
assert d['user_defined_backups'] == {'T0': 'T1'}, f'Expected T0->T1, got {d[\"user_defined_backups\"]}'
print('PASS: user_defined_backups present in to_dict() output')
"
  </verify>
  <done>
    - SHOW_TOOL_FALLBACK_STATE output includes `user_defined_backups` key at top level
    - Format matches state file: {logical_tool: backup_tool_or_null}
    - user_defined_backups is clearly distinguished from configured backups in output
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test SHOW_TOOL_FALLBACK_STATE includes user_defined_backups and verify Phase 10 logging</name>
  <files>tests/test_tool_fallback_extension.py</files>
  <behavior>
    - Test: SHOW_TOOL_FALLBACK_STATE output includes user_defined_backups key
    - Test: user_defined_backups reflects current state (mappings and undefinitions)
    - Test: SHOW_TOOL_FALLBACK_STATE output is valid JSON (json.dumps succeeds)
    - Test: Phase 10's _TOOL_FALLBACK_TN logs via respond_info (verify existing behavior)
  </behavior>
  <action>
    Add tests to `tests/test_tool_fallback_extension.py`:

    1. `test_show_fallback_state_includes_user_defined_backups`:
       - Load extension with user-defined backups set via DEFINE_TOOL_BACKUP
       - Call SHOW_TOOL_FALLBACK_STATE
       - Verify output JSON contains `user_defined_backups` key
       - Verify the value matches the expected mappings

    2. `test_show_fallback_state_user_defined_backups_empty_when_none`:
       - Load extension without any user-defined backups
       - Call SHOW_TOOL_FALLBACK_STATE
       - Verify `user_defined_backups` key exists and is empty dict `{}`

    3. `test_show_fallback_state_user_defined_backups_reflects_undefinition`:
       - Set a user-defined backup, then undefine it
       - Call SHOW_TOOL_FALLBACK_STATE
       - Verify `user_defined_backups` no longer contains the mapping

    4. `test_show_fallback_state_json_serializable`:
       - Call SHOW_TOOL_FALLBACK_STATE
       - Verify output is valid JSON (all values JSON-serializable)

    5. `test_undefined_tool_detection_logs_via_respond_info`:
       - Verify Phase 10's `_TOOL_FALLBACK_TN` logs via `self.gcode.respond_info()` when tool is not configured
       - Check that the info message appears in responses
       - This verifies OBSERVE-02: detection events reach Klipper's log system

    Use existing test patterns: `load_extension` helper, `FakeGCmd`, `printer.gcode.invoke_command()`.
  </action>
  <verify>
    python3 -m pytest tests/test_tool_fallback_extension.py -x -q --tb=short
  </verify>
  <done>
    - 5 new test cases added covering user_defined_backups visibility, empty state, undefinition, JSON serialization, and Phase 10 logging verification
    - All existing tests still pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| get_status() → Klipper console | Status snapshot is read-only; user_defined_backups is already validated at save time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Information Disclosure | SHOW_TOOL_FALLBACK_STATE output | accept | user_defined_backups is operator-visible state; no PII or sensitive data exposed. Shows only tool names (T0, T1, etc.) |
| T-12-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Phase-wide verification:
1. `python3 -m pytest tests/ -q --tb=short` — all tests pass
2. SHOW_TOOL_FALLBACK_STATE output includes user_defined_backups at top level
3. Phase 10's respond_info calls verified as Klipper log-visible
</verification>

<success_criteria>
- SHOW_TOOL_FALLBACK_STATE includes user_defined_backups key (FALLBACK-04, OBSERVE-01)
- Output format: {logical_tool: backup_tool_or_null} matching state file format
- Undefined tool detection events visible in Klipper log system (OBSERVE-02)
- 5 new tests covering visibility, empty state, undefinition, JSON, and logging
- All 406+ existing tests pass (zero regression)
</success_criteria>

<output>
Create `.planning/phases/12-fallback-integration-observability/12-02-SUMMARY.md` when done
</output>
