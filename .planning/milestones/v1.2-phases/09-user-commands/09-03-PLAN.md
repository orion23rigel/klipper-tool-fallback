---
phase: 09-user-commands
plan: 03
type: execute
wave: 3
depends_on:
  - "09-01"
files_modified:
  - klippy/extras/tool_fallback.py
  - tests/test_tool_fallback_extension.py
autonomous: true
requirements:
  - CMD-07
  - CMD-08

must_haves:
  truths:
    - "SHOW_TOOL_BACKUPS lists all user-defined backup mappings"
    - "SHOW_TOOL_BACKUPS displays mappings in human-readable multi-line format"
    - "SHOW_TOOL_BACKUPS shows '(empty)' when no user-defined mappings exist"
  artifacts:
    - path: klippy/extras/tool_fallback.py
      provides: "cmd_SHOW_TOOL_BACKUPS command handler"
      min_lines: 15
  key_links:
    - from: klippy/extras/tool_fallback.py
      to: klippy/extras/tool_fallback_state.py
      via: "cmd_SHOW_TOOL_BACKUPS reads self.state.user_defined_backups (MappingProxyType)"
      pattern: "self\.state\.user_defined_backups"
---

<objective>
Implement `SHOW_TOOL_BACKUPS` G-code command. This is the read-only command that lists all user-defined backup mappings in a human-readable format.

Purpose: Provide operators with visibility into what user-defined backup mappings currently exist. Command follows the existing `cmd_COMMAND_NAME(self, gcmd)` pattern registered via `self.gcode.register_command()` (D-01), uses uppercase command name with underscores (D-02), and gets a `desc` parameter (D-03). Takes no parameters (D-06). Does NOT need workflow guards (D-12) or notification guards (read-only).

Output: One new command handler registered in `__init__`, plus test coverage in `test_tool_fallback_extension.py`.
</objective>

<execution_context>
@/home/orion/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/orion/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-state-schema-extension/08-CONTEXT.md
@.planning/phases/09-user-commands/09-CONTEXT.md
@.planning/phases/09-user-commands/09-01-PLAN.md
@.planning/phases/09-user-commands/09-02-PLAN.md

# Source files for pattern reference:
@klippy/extras/tool_fallback.py
@tests/test_tool_fallback_extension.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement cmd_SHOW_TOOL_BACKUPS</name>
  <files>klippy/extras/tool_fallback.py</files>
  <behavior>
    - Test empty state: SHOW_TOOL_BACKUPS outputs "(empty)" or equivalent when no mappings
    - Test with mappings: outputs each mapping as "Tn -> Tm" in human-readable format
    - Test no guards needed: works during workflow transition and notification (read-only)
    - Test output format matches D-20: multi-line, not JSON
  </behavior>
  <action>
    Implement `cmd_SHOW_TOOL_BACKUPS(self, gcmd)` in `klippy/extras/tool_fallback.py`.

    **Placement:** Add after `cmd_UNDEFINE_TOOL_BACKUP` (after Task 2's method).

    **Registration:** Register in `__init__` after UNDEFINE_TOOL_BACKUP:
    ```python
    self.gcode.register_command(
        "SHOW_TOOL_BACKUPS", self.cmd_SHOW_TOOL_BACKUPS,
        desc="List user-defined backup tool mappings")
    ```

    **Implementation — follow `cmd_SHOW_TOOL_FALLBACK_STATE` (line 272) for the read-only pattern:**
    ```python
    def cmd_SHOW_TOOL_BACKUPS(self, gcmd):
        udd = self.state.user_defined_backups
        if not udd:
            gcmd.respond_info("User-defined backups: (empty)")
            return
        lines = ["User-defined backups:"]
        for logical in sorted(udd):
            backup = udd[logical]
            if backup is not None:
                lines.append("  %s -> %s" % (logical, backup))
            else:
                lines.append("  %s -> (none)" % (logical,))
        gcmd.respond_info("\n".join(lines))
    ```

    **D-20 compliance:** Output is human-readable multi-line format, NOT JSON. Format:
    ```
    User-defined backups:
      T0 -> T1
      T2 -> T3
    ```

    **D-21 compliance:** Shows ONLY user-defined backups (from `self.state.user_defined_backups`, D-22), not configured backups. Per D-21, the "both" in CMD-08 means showing user-defined alongside a note about configured backups being separate — the output header "User-defined backups:" makes this distinction clear.

    **No guards needed (D-12):** Read-only operation, no workflow or notification guards required.

    **D-18 compliance:** Uses `gcmd.respond_info()` for human-readable output.

    **D-23 compliance:** Reads `self.state.user_defined_backups` which is a `MappingProxyType` — immutable view, never mutates.
  </action>
  <verify>
    <automated>python -m pytest tests/test_tool_fallback_extension.py -x -q -k "show_tool_backups" 2>&1 | tail -5</automated>
  </verify>
  <done>
    `cmd_SHOW_TOOL_BACKUPS` exists, is registered, and:
    - Lists all user-defined backup mappings
    - Uses human-readable multi-line format
    - Shows "(empty)" when no mappings exist
    - Does NOT require workflow/notification guards
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests for SHOW_TOOL_BACKUPS</name>
  <files>tests/test_tool_fallback_extension.py</files>
  <behavior>
    - Test registration: "SHOW_TOOL_BACKUPS" in printer.gcode.commands
    - Test empty state: outputs "(empty)" line
    - Test with single mapping: outputs "T0 -> T1"
    - Test with multiple mappings: outputs sorted mappings
    - Test with None backup value: outputs "(none)"
  </behavior>
  <action>
    Write test cases for `SHOW_TOOL_BACKUPS` in `tests/test_tool_fallback_extension.py`.

    **Tests to write (5 tests):**

    1. `test_show_tool_backups_is_registered` — assert "SHOW_TOOL_BACKUPS" in printer.gcode.commands
    2. `test_show_tool_backups_empty_state` — no user-defined backups, response contains "(empty)"
    3. `test_show_tool_backups_single_mapping` — DEFINE T0 T1, SHOW outputs "T0 -> T1"
    4. `test_show_tool_backups_multiple_mappings_sorted` — DEFINE T0 T1 and T2 T3, SHOW outputs sorted
    5. `test_show_tool_backups_with_none_backup` — DEFINE then UNDEFINE T0, SHOW outputs "(none)" for T0

    **Pattern reference — `test_backup_commands_are_registered` (test_tool_fallback_backups.py line 48):**
    - Uses `assert "COMMAND" in set(printer.gcode.commands)` for registration check

    **Pattern reference — `test_set_reorders_and_clears_policy_on_disk` (test_tool_fallback_backups.py line 58):**
    - Uses `invoke()` helper to call commands and checks `extension.state` and `gcmd.responses`

    **Test helper imports:**
    - `load_extension` and `invoke` from `tests/test_tool_fallback_backups.py` (or replicate locally)
    - `FakeGCmd` from `conftest`
  </action>
  <verify>
    <automated>python -m pytest tests/test_tool_fallback_extension.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    5 new test cases pass. All existing tests still pass. SHOW_TOOL_BACKUPS functional.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| G-code → State read | Read-only G-code command reads persistent state — no mutation risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
| T-09-03 | Information Disclosure | cmd_SHOW_TOOL_BACKUPS | accept | Displays only user-defined mappings (low sensitivity), no PII or hardware secrets exposed |
</threat_model>

<verification>
Run full test suite: `python -m pytest tests/ -x -q`
Verify all existing tests pass and new tests pass.
</verification>

<success_criteria>
- `cmd_SHOW_TOOL_BACKUPS` registered and functional
- Human-readable multi-line output format
- Empty state handled correctly
- 5 new test cases covering all scenarios
- All existing tests still pass
</success_criteria>

<output>
Create `.planning/phases/09-user-commands/09-03-PLAN.md` when done
</output>
